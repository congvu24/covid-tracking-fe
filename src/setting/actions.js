import { createAction } from "redux-actions";
export const actionToogle = createAction("TOOGLE_THEME");
export const changeFocus = createAction("CHANGE_FOCUS");
export const closeActive = createAction("CLOSE_ACTIVE");
